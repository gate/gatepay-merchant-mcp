{
  "name": "gatepay-merchant-mcp",
  "version": "1.0.0",
  "description": "GatePay Merchant Local (stdio) MCP server",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "gatepay-merchant-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "node dist/index.js",
    "test": "tsx test/test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "gatepay",
    "payment"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
